# OpenCCVoice 頭切れタイミング調整 — 検証記録

最終更新: 2026-08-21 / 対象: ji2tab/OpenCCVoice-For-DVSwitch (main)

## 0. 結論（確定）

頭切れの原因は **3つ**、いずれも解消済み。

1. **素材 WAV に助走が無かった** — time_intro/001/002 は 20ms で語頭。fixed_intro だけ
   200ms の助走を持ち「起動・応答はきれいで定時だけ欠ける」の直接原因。
2. **ノイズフロアの段差** — 素材1サンプル目から ±255（−42dBFS）。無音から 34dB のジャンプ。→ 20ms フェードイン。
3. **ストリーム先頭の SET_INFO** — keyup=0 の 20ms 後に音声が始まる構成は語頭を欠く（RF 実測）。→ `TX_METADATA_BEFORE_VOICE=False`。

禁止事項:
- **前パディング 0 は禁止** — 先頭トーンが消えると VoiceLCHeader が中継で失われ、受信側は
  送信開始を検出できず幽霊ストリーム化（DroidStar で bot 11.5s が 52s に化けた）。トーンの真の役目はヘッダ保護。
- **EOT 0 は禁止** — keyup=0 が無いと AB がストリームを閉じられない。`max(1,…)` の下限は撤去禁止。値は V1.87 実測に従い 1。

統計（journalctl 45日1390サンプル）: 同一素材の応答 27 回で標準偏差 **0.000** — 送信長は WAV 長で完全に決まる。

## 1. ★ GitHub コミット状況（両エディション完了）

**JTW（コミット済み・HEAD c467fe7 系）**
- `dvswitch_bot.py` **V2.10jtw** / `voice_make.py` **V1.03vmp** / `create_wav.sh` **V1.3**（apply_leads）
- `README.md` 版更新＋タイミング節 / `CHANGELOG_weather_V204jtw.md` 旧値巻き戻し防止バナー
- 構築版と実機 sed 状態の送出 UDP バイト列一致を検証済み。

**VVW（構築完了・会話へ送付済み・コミット待ち）** — 2026-08-21 構築
- `dvswitch_bot.py` V2.07vvw → **V2.08vvw**（分離＋タイミングを1版でまとめる）
- `voice_make.py` V1.02vvm → **V1.03vvm** / `create_wav.sh` V1.32vv → **V1.4vv**（apply_leads）
- `README.md` 版更新＋タイミング節 / `changelog_V2.08vvw.md`
- 送出関数スタブ検証（前25/後6・起動pre=150で+125pkt・EOT=0でも下限1・BEFORE_VOICE切替）＋
  apply_leads end-to-end（語頭2000/520ms）＋ py_compile/bash -n 全通過。
- **⚠ VVW は RF 未検証**（JTW での実測値を移植）。ocv-voicevox 実機で頭欠け・切れ際・幽霊ストリーム
  （DroidStar started/ended と bot 実測の一致）を配置後に必ず確認。
- **VVW 固有**: `STARTUP_PRE_PADDING_PACKETS=150` は据え置き（安全側。RF 確認後に PRE_PADDING と同値へ縮小可）。

## 2. 定数（両エディション共通の到達点）

| 定数 | 旧 | 新 |
|---|---|---|
| PRE_PADDING_PACKETS | （共有85=1.70s） | **25 (0.50s)** ヘッダ保護。0 禁止 |
| POST_PADDING_PACKETS | （共有85=1.70s） | **6 (0.12s)** 終端フラッシュ |
| USRP_EOT_REPEAT | 3 | **1**（0 は下限で禁止） |
| REPLY_TX_LEAD_DELAY_SEC | 1.5 | **0.0**（fixed_intro 助走へ移動） |
| REPLY_TAIL_SEC | — | **1.0** 新設（キャッシュ署名にも追加） |
| TX_METADATA_BEFORE_VOICE | — | **False** 新設 |
| ASSERT_IDENTITY_ON_RX_END | — | **False** 新設 |

vmp/vvm: `LEAD_BEFORE_INTRO_SEC` 0.5→0.0、`SLOT_TAIL_SEC=1.0` 新設。
create_wav apply_leads: fixed_intro `pad 1.78`、time_intro/001/002 `fade t 0.02 pad 0.5`。

## 3. ⚠ ノードの取り違いに注意

| ノード | プロンプト | 版 | 特徴 |
|---|---|---|---|
| ocv-uhf | `ocv@ocv-uhf` | JTW V2.10jtw 相当（実機はsed等価）| 現役 JTW |
| pi-star-14575 | `pi-star@pi-star-14575(rw)` | **V1.92**（JT系・凍結） | voice_make 無し。8/20 誤書換→復旧済み |
| ocv-voicevox | `ocv@ocv-voicevox` | VVW V2.07vvw（V2.08vvw デプロイ対象）| VVW。8/21 に EOT sed を誤実行（空振り・無害） |

**対策**: 実行コマンドの先頭に必ず `hostname`。

## 4. 主要な実測知見

- **リード≡パディング**: WAV内無音と送信パディングは電波上バイト単位で同一。違いは「誰がいつ作るか」「範囲」だけ。
- **SOT は存在しない**: USRP に開始専用パケットは無く keyup 0→1 の最初のパケットが開始。SET_INFO は
  keyup=0 で EOT とヘッダ上ほぼ同形（type だけ違う）— 音声直前に置くと「閉じて即開く」形で語頭を巻き込む。
- **TGIF 食われ量**（旧記録 1.3〜1.8s @AMP150）は ocv-uhf 現経路では当てはまらずほぼゼロ。ヘッダ保護（先頭の非無音）は必須。
- `REPLY_TX_LEAD_DELAY_SEC` 来歴: V1.77=1.5(代理値)→V1.86=1.0→V1.95a=2.5(失効)→V2.05jtw=0.5(実測)→
  V2.08jtw=1.5(実測)→V2.10jtw=0.0(助走へ移行)。
- fixed_intro の助走 2.0s は TX lead 廃止時の機械的移し替えで**最適値は未詰め**（スロットは 0.52s で成立）。
- 第30条10分ID（rule 30）は fixed_intro 直送で `_generate_hybrid` を通らないため REPLY_TAIL_SEC が付かない
  （従来どおり・退行ではない）。次版で経由化を検討可。

## 5. デプロイ手順（共通）

1. 3ファイル（bot / vmp / create_wav）を `/opt/dvswitch_bot/bin/` へ差し替え → `systemctl restart dvswitch-bot`。
2. 一度 `sudo ./create_wav.sh --regen`（記録済み内容で素材を助走付きに焼き直す。JTW=texts / VVW=texts+voice）。
   ※これで実機素材が「手作業sox当て」から「create_wav が正規に焼いた助走入り」へ。急ぎでなければ次に素材を
   作り直すときで可（現行実機の素材は既に助走入り）。
3. 起動アナウンス・次の定時・カーチャンク1回を RF ＋ DroidStar ログで確認。

## 6. 次の課題

1. **VVW（V2.08vvw 一式）を ocv-voicevox へデプロイ → RF 確認 → GitHub コミット**
2. MD-619 での RF 受信確認（歴史的にいちばん敏感な受信機）
3. fixed_intro 助走 2.0s の切り詰め（0.5s まで下げられる可能性）／VVW STARTUP_PRE_PADDING の縮小
4. test_send.py の定数追従（デバッグツール・任意）

## 7. 流儀

- GitHub が常に正本。実測は sed で手当て → 確定後に定数化して版へ焼込み
- 値を変えたら必ず実機で耳確認（理屈で断定しない）
- 版は4箇所同期（タイトル / 要点 / Document Version / `__version__`）
- 1メッセージにつき実行コマンドは1ブロック。**先頭に `hostname` を入れる**
