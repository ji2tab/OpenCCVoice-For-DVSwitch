# 音声ソース作成のSoX処理

DVSwitch bot V1.58 で使用する固定WAVファイル作成時の、SoX処理2パターンの違いと使い分けについて。

---

## 結論

| | パターンA(無音残す) | パターンB(無音トリム) |
|---|---|---|
| 対象 | `fixed_intro.wav`, `fixed_outro.wav` | `time_intro.wav`, `001.wav`, `002.wav` |
| 理由 | **他の音声と連結される**ため | **単独で再生**される、または**動的合成と連結時に無音が邪魔**になるため |

---

## bot側でどう使われるか

V1.58のコードを見ると、3パターンの使われ方があります。

### ① カーチャンク応答(連結あり)

```
[fixed_intro] + [0.5秒の無音] + [動的合成] + [fixed_outro]
```

→ `fixed_intro.wav` と `fixed_outro.wav` は**真ん中に動的合成を挟んで連結**される

### ② 時報(連結あり)

```
[time_intro] + [0.5秒の無音] + [動的合成「〇〇時です」]
```

→ `time_intro.wav` も**動的合成と連結**される

### ③ 定時メッセージ(単独再生)

```
[001.wav] または [002.wav] をそのまま送信
```

→ `001.wav` `002.wav` は**そのまま単独で**送信される

---

## なぜパターンが2つあるのか

①と②はどちらも連結なのに、`fixed_intro`(パターンA・無音残す)と `time_intro`(パターンB・無音トリム)で処理が違います。

正直に言うと、**設計上の必然性というより手順書の作成時期の差**だと思われます。理由を3つ挙げます。

### 理由1:open_jtalk が出力する無音の量

Open JTalkで合成すると、文末に**0.3〜0.5秒程度の無音**がデフォルトで付きます。

- 句点「。」の有無
- テキストの長さ
- 韻律処理の都合

これが残ったまま連結されると、想定外の「間」が生まれてリズムが悪くなることがあります。

### 理由2:V1.58 の `_generate_hybrid()` 内で行う処理

コードを見ると、連結時にこういう処理をしています:

```python
# イントロの末尾に GAP_AFTER_INTRO_SEC(0.5秒)の無音を追加
sox intro TEMP_INTRO_PADDED pad 0 0.5
```

**つまり、bot側で「0.5秒の無音」を意図的に追加しています。**

ということは:

- `fixed_intro.wav` に元々無音が残っていても、その後ろにさらに0.5秒追加されるだけ
- 設計者は「多少の無音差は許容範囲」と判断したか、または無音込みでテスト済み

### 理由3:単独再生される `001.wav` `002.wav` は無音トリムが必須

定時メッセージは**そのまま送信**されるので、ファイル先頭・末尾の無音は:

- 送信開始から実際の音声まで間延びする
- 送信終了後も無音が垂れ流される

→ ユーザー体験が悪いので**必ずトリムすべき**です。

---

## では `fixed_intro.wav` もトリムした方が良いか?

**結論:どちらでもOKですが、トリムした方が綺麗です。**

### トリムするメリット

- イントロ末尾の不規則な無音がなくなり、`pad 0.5` で追加される無音だけになるので**間の長さが一定になる**
- 音声構成の予測がしやすい

### トリムするコマンド(改良版)

**fixed_intro.wav:**

```bash
echo "こちらは、ジェイジェイツーワイワイケー、おわりあさひ ディーエムアール デジピーターです。" | open_jtalk -x /var/lib/mecab/dic/open-jtalk/naist-jdic -m /usr/share/hts-voice/mei/mei_normal.htsvoice -ow /tmp/temp_intro.wav && sudo sox /tmp/temp_intro.wav -r 8000 -c 1 -b 16 /opt/dvswitch_bot/fixed_intro.wav silence 1 0.1 1% reverse silence 1 0.1 1% reverse
```

**fixed_outro.wav:**

```bash
echo "カーチャンクです。" | open_jtalk -x /var/lib/mecab/dic/open-jtalk/naist-jdic -m /usr/share/hts-voice/mei/mei_normal.htsvoice -ow /tmp/temp_outro.wav && sudo sox /tmp/temp_outro.wav -r 8000 -c 1 -b 16 /opt/dvswitch_bot/fixed_outro.wav silence 1 0.1 1% reverse silence 1 0.1 1% reverse
```

---

## ⚠️ 注意点

**実機で聞き比べてから決めるのが一番**です。理由:

1. **間の感じ方は人による**:0.5秒+末尾無音0.3秒 = 0.8秒の「間」が、自然に聞こえるか不自然に聞こえるかは好み
2. **DMRデコーダー側の挙動**:無音が短すぎるとPTT解放のタイミングが繊細になる可能性
3. **既に動いているなら触らない**:現状の構成で問題なく聞こえているなら、無理に変える必要なし

---

## SoXコマンドの意味

### パターンA(単純変換)

```
sox 入力 -r 8000 -c 1 -b 16 出力
```

| オプション | 意味 |
|---|---|
| `-r 8000` | サンプリングレート 8000Hz に変換 |
| `-c 1` | モノラル(1チャンネル) |
| `-b 16` | 量子化ビット数 16bit |

→ DVSwitch/DMRの送信フォーマット(8kHz/mono/16bit PCM)に変換するだけ。

### パターンB(前後トリム付き)

```
sox 入力 -r 8000 -c 1 -b 16 出力 silence 1 0.1 1% reverse silence 1 0.1 1% reverse
```

パターンAに加えて `silence` エフェクトを2回適用:

| 部分 | 意味 |
|---|---|
| `silence 1 0.1 1%` | 先頭の無音をトリム(0.1秒以下・振幅1%以下を無音とみなす) |
| `reverse` | 音声を反転 |
| `silence 1 0.1 1%` | (反転状態で)再び先頭の無音をトリム = 実質的に末尾の無音をトリム |
| `reverse` | 元の向きに戻す |

→ SoXに末尾トリム機能が無いため、「反転→先頭トリム→反転」というイディオムで末尾もトリムしている。

---

## まとめ

| 質問 | 答え |
|---|---|
| なぜパターンが2つあるの? | **手順書作成時期の差**であり、設計上の厳密な理由はない可能性が高い |
| `001.wav` `002.wav` のトリムは必要? | **必須**(単独再生されるため) |
| `time_intro.wav` のトリムは必要? | **推奨**(連結時の「間」を一定にするため) |
| `fixed_intro.wav` `fixed_outro.wav` のトリムは必要? | **どちらでも可。トリムした方が綺麗だが、現状で問題なければそのまま** |

V2.0で投機的合成を試した経緯から、レイテンシや音声の繋がりに敏感になっている場合は、一度トリムなし版で動かしてみて、間が気になればトリム版に差し替える、という進め方が安全です。

---

*Document Version: 1.0*
*Last Updated: May 2026*
*対応bot version: DVSwitch bot V1.58*
