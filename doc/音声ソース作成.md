# DVSwitch bot V1.58 固定WAVファイル作成手順

JJ2YYK デジピーター自動応答システム用の固定WAVファイル作成コマンド集です。

## 固定WAVファイル一覧

| ファイル名 | 読み上げ内容 | 用途 |
|---|---|---|
| `fixed_intro.wav` | こちらは、ジェイジェイツーワイワイケー、おわりあさひ ディーエムアール デジピーターです。 | カーチャンク応答のイントロ |
| `fixed_outro.wav` | カーチャンクです。 | カーチャンク応答のアウトロ |
| `time_intro.wav` | こちらは、ジェイジェイツーワイワイケー、 | 時報のイントロ(後ろに動的合成で「〇〇時です」が続く) |
| `001.wav` | こちらは、ジェイジェイツーワイワイケー、尾張旭、DMR、デジピーターです。オープン、シーシーヴォイス、フォー、ディーブイスイッチからの音声です。 | 定時メッセージ1 |
| `002.wav` | こちらは、ジェイジェイツーワイワイケー、尾張旭、DMR、デジピーターです。ティージーアイエフ、ヨンヨンハチサンサンと、インターネット接続しています。 | 定時メッセージ2 |
| `time_outro.wav` | です。 | ⚠️ V1.58では未使用(将来再利用のため残置) |

---

## 作成コマンド集

### fixed_intro.wav(カーチャンク応答のイントロ)

```bash
echo "こちらは、ジェイジェイツーワイワイケー、おわりあさひ ディーエムアール デジピーターです。" | open_jtalk -x /var/lib/mecab/dic/open-jtalk/naist-jdic -m /usr/share/hts-voice/mei/mei_normal.htsvoice -ow /tmp/temp_intro.wav && sudo sox /tmp/temp_intro.wav -r 8000 -c 1 -b 16 /opt/dvswitch_bot/fixed_intro.wav
```

### fixed_outro.wav(カーチャンク応答のアウトロ)

```bash
echo "カーチャンクです。" | open_jtalk -x /var/lib/mecab/dic/open-jtalk/naist-jdic -m /usr/share/hts-voice/mei/mei_normal.htsvoice -ow /tmp/temp_outro.wav && sudo sox /tmp/temp_outro.wav -r 8000 -c 1 -b 16 /opt/dvswitch_bot/fixed_outro.wav
```

### time_intro.wav(時報のイントロ)※前後トリム入り

```bash
echo "こちらは、ジェイジェイツーワイワイケー、" | open_jtalk -x /var/lib/mecab/dic/open-jtalk/naist-jdic -m /usr/share/hts-voice/mei/mei_normal.htsvoice -ow /tmp/time_intro.wav && sox /tmp/time_intro.wav -r 8000 -c 1 -b 16 /opt/dvswitch_bot/time_intro.wav silence 1 0.1 1% reverse silence 1 0.1 1% reverse
```

### 001.wav(定時メッセージ1)※前後トリム入り

```bash
echo "こちらは、ジェイジェイツーワイワイケー、尾張旭、DMR、デジピーターです。オープン、シーシーヴォイス、フォー、ディーブイスイッチからの音声です。" | open_jtalk -x /var/lib/mecab/dic/open-jtalk/naist-jdic -m /usr/share/hts-voice/mei/mei_normal.htsvoice -ow /tmp/001_raw.wav && sox /tmp/001_raw.wav -r 8000 -c 1 -b 16 /opt/dvswitch_bot/001.wav silence 1 0.1 1% reverse silence 1 0.1 1% reverse
```

### 002.wav(定時メッセージ2)※前後トリム入り

```bash
echo "こちらは、ジェイジェイツーワイワイケー、尾張旭、DMR、デジピーターです。ティージーアイエフ、ヨンヨンハチサンサンと、インターネット接続しています。" | open_jtalk -x /var/lib/mecab/dic/open-jtalk/naist-jdic -m /usr/share/hts-voice/mei/mei_normal.htsvoice -ow /tmp/002_raw.wav && sox /tmp/002_raw.wav -r 8000 -c 1 -b 16 /opt/dvswitch_bot/002.wav silence 1 0.1 1% reverse silence 1 0.1 1% reverse
```

---

## sox処理の違い

2種類のsox処理パターンを使い分けています。

### パターンA(単純変換)

対象: `fixed_intro.wav`, `fixed_outro.wav`

```
sox 入力 -r 8000 -c 1 -b 16 出力
```

8kHz/mono/16bitに変換するだけ。前後の無音はそのまま残る。

### パターンB(前後トリム付き)

対象: `time_intro.wav`, `001.wav`, `002.wav`

```
sox 入力 -r 8000 -c 1 -b 16 出力 silence 1 0.1 1% reverse silence 1 0.1 1% reverse
```

8kHz/mono/16bitに変換 + 前後の無音(0.1秒以下、振幅1%以下)をトリミング。

---

## 共通仕様

- **音声合成エンジン:** Open JTalk(声: メイ・標準)
- **辞書:** `/var/lib/mecab/dic/open-jtalk/naist-jdic`
- **音声モデル:** `/usr/share/hts-voice/mei/mei_normal.htsvoice`
- **出力形式:** 8000Hz / mono / 16bit PCM WAV
- **出力先ディレクトリ:** `/opt/dvswitch_bot/`

---

## 確認コマンド

作成後にファイルが揃っているか確認:

```bash
ls -la /opt/dvswitch_bot/
```

各ファイルのフォーマットを確認:

```bash
soxi /opt/dvswitch_bot/fixed_intro.wav
soxi /opt/dvswitch_bot/fixed_outro.wav
soxi /opt/dvswitch_bot/time_intro.wav
soxi /opt/dvswitch_bot/001.wav
soxi /opt/dvswitch_bot/002.wav
```

期待される出力(各ファイルとも):
- Sample Rate: 8000
- Channels: 1
- Precision: 16-bit

---

## 注意事項

- bashで複数行に分けて書く場合は、行末に `\`(バックスラッシュ)を付ける必要があります。`\` の直後にスペースを入れないこと。
- 一番確実なのはワンライナー(1行にすべて記述)です。
- `time_outro.wav` はV1.58では使用されませんが、V1.57以前との互換のため、または将来の再利用のために定数定義は残っています。

---

*Document Version: 1.0*
*Last Updated: May 2026*
*対応bot version: DVSwitch bot V1.58*
