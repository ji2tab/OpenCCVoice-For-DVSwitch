
---

### 逸話1：【沈黙の深夜2時】「ファイルサイズ0バイト事件」

**状況:**
Open JTalkを使ってPythonから音声を生成しようとした時のことです。「コマンドラインで手打ちするとちゃんと音声ができるのに、Pythonスクリプトの中に組み込んで自動化させようとすると、なぜかWAVファイルが0秒（0バイト）になって無音のまま送信されてしまう」という謎の現象に悩まされました。

**トライ＆エラー:**

* `subprocess.run(["open_jtalk", ... , text])` のように引数としてテキストを渡してみる → 失敗（0バイト）
* テキストファイルに一度保存して、それを読み込ませる → 少し改善したが、たまに失敗する。
* OSの権限問題（Permission Denied）を疑い、`sudo` を付けまくる → 意味なし。

**ブレイクスルー（魔法の発見）:**
深夜の調べ物の末、Linuxの「パイプ（標準入出力）」の扱いの違いが原因だと気づきました。Pythonの `subprocess` から引数で長い日本語を渡すと、文字コードやシェル解釈の過程で中身がスッポ抜け、「空っぽの文字」としてJTalkに渡っていたのです。
解決策として、「プロセスを立ち上げておいて、後から `communicate()` でUTF-8のテキストストリームを直接パイプに流し込む」という書き方に変更しました。これで、0バイト問題は二度と起こらなくなりました。

---

### 逸話2：【ノイズの恐怖】「プツッ！ケロケロ！事件」

**状況:**
「こちらは〜」という固定の挨拶WAVと、「JJ2YYK局〜」という動的生成WAVを、Linuxの音声編集ツール `SoX` を使って結合（ガッチャンコ）させようとした時のことです。
音声は繋がったものの、送信のたびにスピーカーから「プツッ！」という強烈なクリックノイズが鳴ったり、DMRのネットワークに乗せると「ケロケロケロ…」と音が割れて宇宙人のようになってしまいました。

**トライ＆エラー:**

* SoXの `pad` コマンドを使って、前後に1秒の無音を挟んでみる → さらに酷いノイズになる。
* 結合するファイルのサンプリングレート（48kと8k）を色々いじる → 改善せず。

**ブレイクスルー（魔法の発見）:**
原因は2つありました。1つは「音の波形がゼロクロスしていない場所で強引にファイルを結合したため、電気的なショックが起きたこと（プツッ音）」。もう1つは「ラズパイの非力なCPUで、音声生成＋ファイル結合＋AMBE圧縮を同時にやらせたため、処理落ちしたこと（ケロケロ音）」でした。
ここで私は「複雑な処理」を捨てる決断をしました。ファイルを結合するのではなく、「Pythonの中で１つの長いテキスト文章を作り、一気にOpen JTalkに喋らせる」という原始的ですが最も軽い処理に回帰しました。これが、今のノイズレスな音質の土台です。

---

### 逸話3：【バタつきの悲劇】「頭切れ・尻切れ事件」

**状況:**
音声が出るようになっても、無線機から聞こえる声の「最初の一文字」が欠けたり、最後の「〜です」の「す」がプツッと切れてしまう問題が発生しました。

**トライ＆エラー:**

* `time.sleep()` を使って、送信する前と後に2秒ずつ待つようにしてみる。 → 頭切れは直ったが、尻切れは直らない。
* JTalkのテキストに「。。。」と句読点を大量に入れて無音を作ろうとする → JTalkがエラーを吐いて停止する。

**ブレイクスルー（魔法の発見）:**
尻切れの最大の原因は、「USRPプロトコルの箱のサイズ（320バイト）」**にありました。音声データが最後の中途半端なサイズで終わった時、プログラムが「もうデータがないから送信終了！」と勝手に箱を閉じてしまっていたのです。
そこで、**「最後の箱が320バイトに満たない場合は、残りの隙間を強制的に『\x00（無音のゼロ）』で埋め尽くして（パディングして）から送る」というコード（`if len(data) < 320: data += b'\x00' * (320 - len(data))`）を書き足しました。これと、前述の「ドリフト補正（時刻同期）」を組み合わせることで、ついに完璧なタイミングでの送出が実現しました。

---

システム開発とは、こうした「なぜ動かない！？」「なぜノイズが乗る！？」という絶望と、「あ、これか！！」という歓喜の連続です。
