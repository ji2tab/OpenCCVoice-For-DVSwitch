
---

### 🧂 システムを仕上げる4つの隠し味（小ネタ集）

#### 1. 【発音の小ネタ】読点（、）が生み出す「無線の間合い」

* **内容:** 辞書（`USER_DICT`）の登録を「ジェイジェイツー…」ではなく「ジェイ、ジェイ、ツー、ワイ、ワイ、ケー」としたこと。
* **理由:** Open JTalkは賢いので、読点（、）を見つけるとほんの一瞬だけ「タメ（ポーズ）」を作ります。この小さなタメが、アマチュア無線家がフォネティックコードやコールサインを読み上げる時の「独特のリズム（間合い）」を完璧に再現してくれます。ただの棒読みロボットから、「分かっている局」の声に変わる小さな魔法です。

#### 2. 【運用の小ネタ】深夜0時の「ログ日替わり」自動追従

* **内容:** `get_latest_log()` 関数の中で `glob` と `getctime` を使っている部分。
* **理由:** Pi-Star（MMDVM）のログファイルは `MMDVM_Bridge-2026-05-13.log` のように毎日日付が変わります。ファイル名を固定してしまうと、深夜0時を過ぎた瞬間にボットが「耳ツンボ」になってしまいます。最新のファイルを自動で探し出すこの数行のおかげで、人間がラズパイを再起動しなくても、毎日勝手に新しいログを読みに行ってくれます。

#### 3. 【通信の小ネタ】音の尻切れを防ぐ「ゼロ埋め（パディング）」

* **内容:** 送信ループの中にある `if len(data) < 320: data += b'\x00' * (320 - len(data))` という暗号のような一行。
* **理由:** DVSwitchに送るUSRPプロトコルは、「絶対に1回320バイトの荷物で送ってこい」という厳しいルールがあります。しかし、音声データが最後にピッタリ320バイトで終わるとは限りません。余った隙間を「無音（ゼロ）」で綺麗に埋めて箱を閉じることで、音声の最後の一文字が欠ける（尻切れになる）のを防いでいます。

#### 4. 【安全の小ネタ】電波の出しっぱなしを防ぐ「絶対PTT OFF」

* **内容:** `try...finally:` ブロックで囲み、最後に `header = struct.pack('!4sIIIIIII', b'USRP', seq, 0, 0, 0, 0, 0, 0)` を送っている部分。
* **理由:** プログラムを `Ctrl+C` で止めたり、何らかのエラーで強制終了したとき、送信状態のまま落ちると「デジピーターがずっと電波を出しっぱなし（送信ロック）」になる大事故に繋がります。Pythonの `finally` を使うことで、スクリプトが死ぬ間際でも「絶対にPTTを離す（送信終了信号を送る）パケット」だけは死に物狂いでDVSwitchに叩きつけるように設計しています。

---

このあたりの細かな気配りは、「とりあえず音が鳴ればいい」というレベルを超えて、「実際の無線インフラとして運用すること」を前提にした泥臭いノウハウです。
