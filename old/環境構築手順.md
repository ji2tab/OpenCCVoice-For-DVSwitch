
---

# DVSwitch ログ監視・自動音声応答システム 構築マニュアル V1.58 対応版

> ⚠️ **【重要】このマニュアルは旧版（V1.58 + pi-star）の記録です**
>
> 本書は単一スクリプト **`dvswitch_bot158.py`（V1.58）** を、**pi-star** ユーザの
> ホームに置く構成の記録です。その後システムは次のように刷新されました:
> - スクリプトを **デーモン本体 `dvswitch_bot.py` ＋ 設定ツール `bot_setup.py`** に分離
>   （systemd 常駐時の `EOFError` 回避と、設定不正時のフェイルセーフを実現）
> - 全スクリプトを **`/opt/dvswitch_bot/bin/`** に集約
> - 検証環境を **Raspberry Pi OS (Bookworm) + DVSwitch-Server / ocv ユーザ**に移行
>
> **これから新規に構築する場合は、最新の手順書を参照してください:**
> - **`Bookwormベース検証手順.md`**（Bookworm + DVSwitch-Server の実機検証手順）
> - **`xyz.md`**（完全マニュアル V3.0 系・バックアップ/リストア章つき）
>
> 本書は、V1.58 時代の設計思想（なぜログ監視か／なぜ USRP か／なぜ Open JTalk + SoX か）
> の解説として価値があるため保存しています。コマンド中の `pi-star` ユーザや
> `dvswitch_bot158.py` というファイル名、`~/`（ホーム）配置は**当時のもの**で、
> 現行リポジトリの構成（`ocv` ユーザ / `dvswitch_bot.py` / `/opt/dvswitch_bot/bin/` 配置）
> とは異なる点に注意してください。

## 第1部：システムの全体像と「なぜこの仕組みなのか」

このシステムは、DVSwitch（Analog_Bridge）とPythonスクリプトを連携させ、特定の交信が終わった後に自動で音声アナウンス（デジピーターの応答など）をネットワークへ送出するbotシステムです。

### なぜ「ログ監視」なのか？（VOXや音声検知との違い）

従来、自動応答を行う場合は「スピーカーから音が出なくなったこと（無音）」を検知するVOX（音声起動）方式が主流でした。しかし、この方式では「ノイズでの誤検知」や「無音の瞬間に途切れるバタつき」が発生します。
本システムでは、DVSwitchの心臓部が出力する「MMDVM_Bridgeの動作ログ（テキスト）」をリアルタイムで直接監視します。デジタルデータとしての「通信開始（header）」と「通信終了（end of voice）」を文字列として正確にキャッチするため、誤動作が一切なく、完璧なタイミングでの応答が可能になっています。

### なぜ「UDP (USRPプロトコル)」なのか？

仮想的なオーディオケーブル（仮想サウンドカード）を使うと、OSの音声ミキサーの設定が非常に複雑になります。
本システムは、生成したWAVファイルのデータを細かく切り刻み、USRPという通信プロトコルにパッケージングして「ネットワーク通信（UDP）として直接DVSwitchのポートに流し込む」手法をとっています。これにより、サウンドカードの相性問題から完全に解放されました。

### なぜ「Open JTalk + SoX」なのか？

DMRのネットワークに音声を流すには、**「8000Hz, モノラル, 16bit」の純粋なPCM音声データ**である必要があります。

1. **Open JTalk**: 外部インターネット（API）に依存せず、Raspberry Pi等のローカル内で瞬時にテキストを音声化します。
2. **SoX**: Open JTalkが作った高品質な音声（48000Hz）を、**Analog_Bridge（USRP プロトコル）の入力仕様**（8000Hz, モノラル, 16bit PCM）に劣化なく、かつ一瞬で正確に変換するために必須のツールです。

---

## 第2部：環境構築手順（ゼロから作る場合）

### Step 1: 必要なパッケージのインストール

音声合成エンジン（Open JTalk）と、音声変換ツール（SoX）をOSにインストールします。

```bash
sudo apt-get update
sudo apt-get install -y open-jtalk open-jtalk-mecab-naist-jdic hts-voice-nitech-jp-atr503-m001 sox

```

### Step 2: 高品質音声モデル「メイ」の導入

標準のロボット声ではなく、人間らしく流暢な女性の声（MMDAgent メイ）をシステムに組み込みます。

```bash
# 作業ディレクトリへ移動
cd ~
# モデルのダウンロードと解凍
wget -L --content-disposition https://sourceforge.net/projects/mmdagent/files/MMDAgent_Example/MMDAgent_Example-1.8/MMDAgent_Example-1.8.zip
unzip MMDAgent_Example-1.8.zip

# 配置先ディレクトリの作成とコピー
sudo mkdir -p /usr/share/hts-voice/mei
sudo cp MMDAgent_Example-1.8/Voice/mei/mei_normal.htsvoice /usr/share/hts-voice/mei/

# 一時ファイルの掃除
rm -rf MMDAgent_Example-1.8 MMDAgent_Example-1.8.zip

```

### Step 3: DVSwitch (Analog_Bridge) の設定確認

Analog_Bridgeが外部からの音声（USRP）を受け付けるようになっているか確認します。
`/opt/Analog_Bridge/Analog_Bridge.ini` を開き、`[USRP]` セクションが以下のようになっていることを確認します。

```ini
[USRP]
rxPort = 51000
txPort = 51001
address = 127.0.0.1

```

※変更した場合は `sudo systemctl restart Analog_Bridge` で再起動します。

---

## 第3部：自動応答スクリプトの実装

安定動作が確認されている **V1.58（dvswitch_bot158.py）** を配置します。

### Step 1: ボット用ディレクトリの作成

```bash
# ボット用ディレクトリを作成（初回のみ）
sudo mkdir -p /opt/dvswitch_bot
sudo chown pi-star:pi-star /opt/dvswitch_bot
```

### Step 2: スクリプトの配置

リポジトリから `dvswitch_bot158.py` をホームディレクトリに配置します。

```bash
# Git / scp / USB など任意の方法でコピー後、実行権限を付与
chmod +x ~/dvswitch_bot158.py
```

### Step 3: 固定 WAV ファイルの作成

V1.58 では以下の固定 WAV ファイルが必要です。詳細な作成コマンドは **「音声ソース作成.md」** を参照してください。

| ファイル | 配置先 | 用途 |
|---|---|---|
| `fixed_intro.wav` | `/opt/dvswitch_bot/` | カーチャンク応答のイントロ |
| `fixed_outro.wav` | `/opt/dvswitch_bot/` | カーチャンク応答のアウトロ |
| `time_intro.wav` | `/opt/dvswitch_bot/` | 時報のイントロ |
| `001.wav` | `/opt/dvswitch_bot/` | 定時メッセージ1 |
| `002.wav` | `/opt/dvswitch_bot/` | 定時メッセージ2 |

作成後、ファイルが揃っているか確認します。

```bash
ls -la /opt/dvswitch_bot/
```

### Step 4: 実行方法

```bash
python3 ~/dvswitch_bot158.py
```

起動時に以下を対話設定できます（Enter でデフォルト値を使用）。

- **最小受信時間**（デフォルト: 0.1 秒）— これより短い送信はカーチャンクと判定しない
- **最大受信時間** — これより長い送信は通常交信とみなす
- **1時間あたりの定時放送回数**（デフォルト: 1 回）— 0 で定時放送無効

### Step 5: systemd への登録（常駐起動）

```bash
sudo nano /etc/systemd/system/dvswitch-bot.service
```

以下を貼り付けて保存:

```ini
[Unit]
Description=DVSwitch Bot V1.58 (OpenCCVoice)
After=network.target Analog_Bridge.service MMDVM_Bridge.service md380-emu.service
Wants=Analog_Bridge.service MMDVM_Bridge.service md380-emu.service

[Service]
Type=simple
ExecStart=/usr/bin/python3 /home/pi-star/dvswitch_bot158.py
Restart=on-failure
RestartSec=10
User=pi-star
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
```

```bash
sudo systemctl daemon-reload
sudo systemctl enable dvswitch-bot
sudo systemctl start dvswitch-bot
sudo systemctl status dvswitch-bot
```

💡 **トラブルシューティング（開発中の教訓）**

**音声が出ない（無音になる）問題について**
- **原因:** md380-emu が停止しているか、Analog_Bridge の設定ミス
- **確認:** `sudo systemctl status md380-emu` で稼働を確認。停止していれば `sudo systemctl restart md380-emu` を実行

**音声がプツプツ・ケロケロになる問題について**
- **原因:** UDP パケット送信のドリフト蓄積（単純な `time.sleep()` の積み重ね）
- **V1.58 での解決:** `time.monotonic()` ベースの絶対時刻同期方式を採用。処理時間のブレを自動吸収して Analog_Bridge へ安定供給します

**Open JTalk でテキストが空になる問題について**
- **原因:** `subprocess.run(input=...)` でテキストを渡すとすっぽ抜ける場合がある
- **解決策:** `subprocess.Popen` と `communicate()` で標準入力ストリームから渡す方式（V1.58 に実装済み）

**ログファイルの日付変更後に動かなくなる問題について**
- **V1.58 での解決:** `ROTATION_CHECK_INTERVAL`（5秒）ごとにログファイルの inode を確認し、変化があれば自動切替。cron による再起動は不要です

---

*(Document Updated: May 2026 / dvswitch_bot158.py V1.58 対応)*
